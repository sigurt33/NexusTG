# Fallback на pip, если uv не получается поставить.
# Использовать так:
#   python -m venv .venv
#   .\.venv\Scripts\activate
#   pip install -r requirements-windows.txt
#   python -m app.cli login

telethon>=1.43
aiosqlite>=0.22
openai>=2.0
fastapi>=0.115
uvicorn[standard]>=0.32
jinja2>=3.1
python-multipart>=0.0.20
python-dotenv>=1.0
rapidfuzz>=3.10
windows-toasts>=1.1; sys_platform == "win32"
tzdata>=2024.2; sys_platform == "win32"
